export class KnowledgeHub{summarize(text:string){const s=text.replace(/\s+/g,' ').trim().split(/(?<=[.!?]) /).slice(0,5).join(' ');return s||'No readable text found.'}flashcards(text:string){return this.summarize(text).split(/(?<=[.!?]) /).filter(Boolean).map((x,i)=>({q:`Key idea ${i+1}`,a:x}))}mindmap(text:string){return this.summarize(text).split(/[,.;]/).filter(Boolean).slice(0,8)}}
